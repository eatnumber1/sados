This is the branch containing Christine's code.

It was not integrated with Russ's because it doesn't function correctly and
if this code was added, Russ's working code would start using the non-working
functions rather than the testing functions, effectively breaking the
filesystem.